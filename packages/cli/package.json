{
  "name": "@necolo/cursor-rules",
  "version": "0.2.2",
  "type": "module",
  "description": "CLI tool for managing Cursor AI rules across projects",
  "bin": {
    "cursor-rules": "./bin/cursor-rules.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "bin",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "prepublishOnly": "cp ../../README.md . && pnpm build",
    "postpublish": "rm -f README.md"
  },
  "dependencies": {
    "@necolo/cursor-rules-data": "workspace:*",
    "cac": "^6.7.14",
    "picocolors": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vitest/coverage-v8": "^1.1.0",
    "typescript": "^5.3.0",
    "vitest": "^1.1.0"
  }
}