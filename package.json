{
  "name": "cursor-rules-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "watch": "pnpm --filter rulink watch",
    "dev": "pnpm --filter rulink dev",
    "test": "pnpm --filter rulink test",
    "test:run": "pnpm --filter rulink test:run",
    "test:coverage": "pnpm --filter rulink test:coverage",
    "changeset": "changeset",
    "test-release": "pnpm build && changeset publish --dry-run"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^2.27.1",
    "typescript": "^5.3.0"
  }
}